<% shop = Shop.find(invoice.shop_id) %>
<% request = Request.find(invoice.request_id)%>
<li>

  Founded a shipper for <%= link_to "#{request.address}", user_shop_request_path(invoice.user_id, invoice.shop_id, request.id) %> request

</li>
